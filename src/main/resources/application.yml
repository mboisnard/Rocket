spring:
  main:
    banner-mode: 'off'

logging:
  level:
    org.springframework: WARN
    org.dizitart: WARN

info:
  rocket:
    commands:
      init: 'init'
      add: 'add'
      status: 'status'
      commit: 'commit'
    nitrite:
      db: '.rocket'
      user: 'rocket'
      password: 'rocket'